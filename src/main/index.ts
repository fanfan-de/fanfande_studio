//  **编写主进程 (`src/main/index.ts`)**：
//     *   初始化窗口。
//     *   **关键点**：设置 IPC (Inter-Process Communication) 监听。因为 AI Agent 可能需要读写本地文件、调用 Python 脚本或执行系统命令，这些必须在主进程完成。

// 主进程入口是 Electron 应用的核心启动器，负责初始化所有系统服务和协调应用生命周期。它作为整个应用的指挥中心，确保各个服务按正确顺序启动和关闭。

/*
1. 主进程入口模块 (src/main/index.ts)
功能概述： 主进程入口是 Electron 应用的核心启动器，负责初始化所有系统服务和协调应用生命周期。它作为整个应用的指挥中心，确保各个服务按正确顺序启动和关闭。

核心功能：

应用初始化：配置硬件加速、窗口动画、协议处理等底层设置
服务协调：按依赖关系启动窗口管理、托盘、API 服务器、AI 代理等服务
生命周期管理：处理应用启动、激活、退出等完整生命周期事件
错误处理：全局异常捕获和渲染进程崩溃恢复
协议处理：支持 cherry-studio:// 自定义协议和深链接
关键技术实现：

单例锁：通过 app.requestSingleInstanceLock() 确保单实例运行
跨平台适配：针对 Windows、macOS、Linux 的不同配置处理
延迟启动：根据配置决定是否自动启动 API 服务器
资源清理：在退出时有序关闭所有服务，防止资源泄漏
关键文件：

src/main/index.ts - 主入口文件
src/main/bootstrap.ts - 引导程序，处理早期初始化
src/main/constant.ts - 平台相关常量定义
架构特点：

采用依赖注入模式管理服务实例
事件驱动架构，响应系统级事件
模块化服务设计，每个服务独立且可测试
*/