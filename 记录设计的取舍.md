动态修改最开始的 System Prompt 还是   System Message（Message 串行注入）
抛弃plan mode？plan不足以支撑一个session的生命周期，会引起上下文精神分裂

agent mode 在一个会话中保持一致，只在system prompt中静态定义，后续无需修改


所谓的进入plan mode其实就是一个（某个turn 的 plan prompt+工具调用限制）

可以有一个plan mode的agent
但是session运行中，改成计划模式，本质上应该是传一个system prompt
