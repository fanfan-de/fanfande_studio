session：以sessionID为键值的若干个对象
const data: Record<

            string,//sessionID

            {

                abort: AbortController//AbortController 对象 - 用于发出取消信号

                callbacks: {

                    resolve(input: Message.WithParts): void

                    reject(): void

                }[]

            }//
           
const state = Instance.state(() => {

  const data: Record<string, Info> = {}

  return data

})